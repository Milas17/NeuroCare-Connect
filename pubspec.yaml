name: neurocare_connect
description: NeuroCare Connect - prototype
publish_to: 'none'
version: 0.0.1

environment:
  sdk: '>=2.18.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.5
  provider: ^6.0.5
  url_launcher: ^6.1.10
  file_picker: ^5.2.6
  intl: ^0.18.0
  http: ^1.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  test: ^1.21.0

flutter:
  uses-material-design: true
  assets:
    - assets/data/webinars.json